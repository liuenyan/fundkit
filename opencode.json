{
  "$schema": "https://opencode.ai/config.json",
  "lsp": true,
  "mcp": {
    "pw": {
      "type": "local",
      "command": ["playwright-mcp", "--no-sandbox", "--browser", "chromium"],
      "environment": {
        "PLAYWRIGHT_BROWSERS_PATH": "{env:HOME}/.cache/ms-playwright"
      },
      "enabled": true
    }
  }
}
